import { Text, View } from 'react-native';

const add = () => {
	return (
		<View className="flex-1 items-center justify-center">
			<Text>Page doesnt exist</Text>
		</View>
	);
};

export default add;
